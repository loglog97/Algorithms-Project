package main;

public class quickSort {
    public static void sort(int[] mainArray, int beg, int end){
        int midPoint = mainArray.length;
        int midLeft = midPoint / 2;
        int midRight = midLeft + midPoint;
        if(beg < end){
            int pivot = (int)Math.floor((midLeft + midPoint + midRight) / 3);
            
        }
    }
}
